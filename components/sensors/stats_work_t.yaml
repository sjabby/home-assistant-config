---
platform: history_stats
name: work_hours_today_person1
entity_id: device_tracker.person1s_iphone_x_beta
state: 'Work'
type: time
start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
end: '{{ now() }}'
